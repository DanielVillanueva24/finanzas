# Este archivo es solo una referencia rapida.
# La configuracion real vive en dos archivos:
#   backend/.env    (copiado de backend/.env.example)
#   frontend/.env   (copiado de frontend/.env.example)

# --- backend ---
DATABASE_URL=sqlite:///./finanzas.db
SECRET_KEY=cambia-esta-llave-en-produccion-por-una-aleatoria-larga
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
SEED_DEMO_DATA=true

# --- frontend ---
VITE_API_URL=http://localhost:8000
